#!groovy

pipeline {
    agent any
    stages {
        stage('do something') {
            steps {
                sh 'ps ax'
            }
        }
        stage('do something else') {
            steps {
                sh 'echo jack sucks balls'
            }
        }
    }
}
